import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  newsItem: {
    height: 45,
    flexDirection: 'row',
    paddingVertical: 10,
    paddingLeft: 12,
    backgroundColor: '#fff',
    alignItems: 'center'
  },
  newsItemTextWrap: {
    flex: 1,
  }
});
export default styles